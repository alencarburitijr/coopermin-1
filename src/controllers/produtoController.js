import { Produto } from "../models/Produto.js";

export const buscaProdutos = (req, res) => {
    res.json({ error: false, inicio: 'todos os produtos'});
}

export const buscaProduto = (req, res) => {
    res.json({ error: false, id_cliente});
}

export const buscaProdutosCliente = (req, res) => {
    res.json({ error: false, id_cliente});
}

export const adicionaProduto = (req, res) => {
    res.json({ error: false, id_cliente});
}

export const updateProduto = (req, res) => {
    res.json({ error: false, id_cliente});
}

export const deleteProduto = (req, res) => {
    res.json({ error: false, id_cliente});
}